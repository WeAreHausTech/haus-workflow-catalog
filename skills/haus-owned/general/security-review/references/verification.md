# Verification

Before handoff:

1. List every file read.
2. List checks you did **not** run (e.g. dependency CVE scan, full SAST).
3. If user should run commands, name them; do not claim output you did not see.

Align with Haus: no secret file reads; no dangerous shell suggestions.
