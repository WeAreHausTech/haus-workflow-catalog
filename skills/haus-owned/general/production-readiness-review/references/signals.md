# Signals

Inspect when relevant to the release unit:

- Env and config: prod vs staging keys, feature flags, secrets handling (paths only)
- Migrations and schema: forward/backward compatibility notes
- Runtime: health/readiness hooks, graceful shutdown, timeouts
- Observability: logging context, error boundaries, alert-worthy failures

Skip sections that do not apply; say **N/A** with reason.
