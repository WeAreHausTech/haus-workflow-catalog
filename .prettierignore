node_modules/
package-lock.json
schema/
# Catalog item trees (skills, commands, agents) — content, not repo plumbing.
# Curated copies must stay byte-identical for drift detection; haus-owned markdown
# is intentionally authored and should not be reformatted by CI.
skills/
commands/
agents/
